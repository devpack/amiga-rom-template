# Simple Amiga ROM template: compile m68k on Linux, run on FS-UAE or real device

git clone https://github.com/devpack/amiga-rom-template.git  
cd amiga-rom-template  
make  
make run  
make clean  

Assume fs-uae is in the $PATH
